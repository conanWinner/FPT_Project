server:
  port: 8889
  address: localhost
  servlet:
    context-path: /

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/fpt"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect




